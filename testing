#include <lab8part1.c>
#include <Scoringfunctions.c>


int main(void){
    
    char board[26][26];
    int n = 8;
    setBoard(board, n);
    setIntermediate(board);
    printf("\n\ninput: ");
    
 
        int i1, i2;
        scanf(" %d%d", &i1, &i2);
        
        int score = countBulkIncreaseScore(board, n, i1, i2, 'B');

        printf("Your score is:%d", score);
    
    
    return 0;
}

